name: 'action test for marketplace'
description: 'lighthouse action'
# inputs:
  # who-to-greet:  
    # description: 'Who to greet'
    # required: true
    # default: 'World'
# outputs:
  # time:
  #   description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'src/index.js'
